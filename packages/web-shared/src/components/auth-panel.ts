/**
 * Brand gradient the restyled auth screens put behind the intro panel.
 *
 * It belongs on the split page's left half rather than inside `AuthIntroPanel`,
 * because the panel renders into a width-capped inner column and would leave
 * the rest of the half unpainted.
 */
export const authGradientPanelClass =
  "bg-[linear-gradient(135deg,#6e3aa0_0%,#2e1442_100%)]";
